# 安全边界

用户 Python 在浏览器独立的 opaque-origin iframe + Worker 中通过 Pyodide 执行，不在 FastAPI 主进程执行。CSP 限制到本地运行资源路径，执行 10 秒超时并可主动停止；没有严格内存配额，不能视作生产级多租户沙箱。浏览器 CSP 和交互验收尚未完成。旧 /api/run 仅返回 mock 检查，不执行代码。

API Key 只存在页面内存和请求作用域，不持久化或导出。后端屏蔽上游凭据错误细节，限制服务 URL、重定向和请求来源。当前仅供可信本机使用，无身份认证，不可直接暴露公网。模型请求会把当前学习内容和代码发送给用户填写的服务商。

Mod 仅声明数据，不获得主进程执行能力。模型反馈不视作已验证学习证据。公开发布前需建立正式安全报告渠道。
